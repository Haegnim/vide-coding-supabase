# index.binding.hook.ts 구현 체크리스트

## ✅ 완료된 작업

### 1. 조회 시나리오

#### 1-1) Supabase 데이터 조회

- ✅ **접속키**: ANON 키 사용 (Supabase 클라이언트에서 자동으로 사용)
- ✅ **테이블명**: `magazine` 테이블에서 조회
- ✅ **조회 데이터 필드**:
  - `id`: uuid ✓
  - `image_url`: 무시 (조회하지 않음) ✓
  - `category`: string ✓
  - `title`: string ✓
  - `description`: string ✓
  - `tags`: string[] | null ✓
- ✅ **조건**: 10개 제한 (`limit(10)`)

#### 1-2) 실제 데이터로 교체

- ✅ Mock 데이터(`articleData`) 제거
- ✅ `useMagazineList` 훅으로 실제 Supabase 데이터 조회
- ✅ 로딩 상태 처리 구현
- ✅ 에러 상태 처리 구현
- ✅ 조회된 데이터를 화면에 표시

#### 1-3) 상세페이지 이동

- ✅ 카드 클릭 시 이벤트 핸들러 구현
- ✅ 이동 주소: `/magazines/[id]` 형식으로 라우팅

## 📁 구현된 파일

### 1. `src/app/magazines/index.binding.hook.ts`

```typescript
- useMagazineList() 커스텀 훅 생성
- useState, useEffect로 데이터 관리
- Supabase에서 magazine 테이블 조회
- magazines, isLoading, error 상태 반환
```

### 2. `src/app/magazines/page.tsx` (수정)

```typescript
- useMagazineList 훅 import 및 사용
- handleMagazineClick 함수로 클릭 이벤트 처리
- 로딩 상태 UI 추가
- 에러 상태 UI 추가
- magazine.description 필드로 데이터 바인딩
- magazine.tags 배열 안전하게 처리 (null 체크)
```

## 🔍 주요 구현 사항

1. **데이터 구조 변경**

   - 기존: `articleData` (mock 데이터)
   - 변경: `magazines` (Supabase 실제 데이터)

2. **필드 매핑**

   - `article.summary` → `magazine.description`
   - `article.content` 필드는 목록에서 제거 (상세페이지 전용)
   - `article.imageUrl` → 기본 이미지 URL 사용 (image_url 필드 무시)

3. **사용자 경험 개선**
   - 로딩 중 메시지 표시
   - 에러 발생 시 사용자 친화적 메시지 표시
   - 카드 클릭 시 커서 모양 변경 (pointer)
   - 상세페이지로 자연스러운 이동

## 🎯 테스트 권장 사항

1. **정상 동작 테스트**

   - [ ] 페이지 로드 시 데이터가 정상적으로 표시되는지 확인
   - [ ] 최대 10개의 매거진만 표시되는지 확인
   - [ ] 카드 클릭 시 해당 ID의 상세페이지로 이동하는지 확인

2. **에러 처리 테스트**

   - [ ] 네트워크 오류 시 에러 메시지가 표시되는지 확인
   - [ ] Supabase 연결 실패 시 적절한 에러 처리 확인

3. **데이터 무결성 테스트**
   - [ ] tags가 null일 경우 오류 없이 처리되는지 확인
   - [ ] 빈 배열이 반환되어도 정상 작동하는지 확인

## ⚠️ 참고 사항

- Next.js의 `<Image />` 컴포넌트 대신 `<img>` 태그를 사용하고 있어 경고가 발생할 수 있습니다.
- 이미지 최적화가 필요한 경우 `next/image`의 `Image` 컴포넌트로 교체를 권장합니다.
- 현재 모든 카드에 동일한 기본 이미지를 사용하고 있습니다 (image_url 필드 무시 요구사항에 따름).
